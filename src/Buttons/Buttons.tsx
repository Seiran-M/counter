import React from "react";import style from "./style.module.css";type ButtonsPropsType = {    inc: () => void    reset: () => void    count: number}export const Buttons = (props: ButtonsPropsType) => {    return (        <div className={style.buttons}>            <button className={style.incButton} onClick={props.inc} disabled={props.count === 5}>inc</button>            <button className={style.resetButton} onClick={props.reset} disabled={props.count === 0}>reset</button>        </div>    )};